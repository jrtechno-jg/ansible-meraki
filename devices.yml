- name: Playbook to query devices
  connection: local
  hosts: localhost
  meraki_device:
    dashboard: dashboard.meraki.com
    auth_key: '{{ MERAKI_KEY }}'
    org_name: JRTECHNOLOGY
    state: query
  delegate_to: localhost
